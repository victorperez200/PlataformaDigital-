@model IEnumerable<CapaDatos.Database.Entregables>

@{
    ViewBag.Title = "Tareas Entregadas";
}

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <div class="d-flex justify-content-between align-items-center">
                <h3 class="mb-0">
                    <i class="fas fa-file-upload me-2"></i>@ViewBag.Title
                </h3>
                @Html.ActionLink("Nueva Entrega", "Create", null, new { @class = "btn btn-light btn-sm" })
            </div>
        </div>

        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>Documento</th>
                            <th>Tipo</th>
                            <th>Fecha</th>
                            <th>Matrícula</th>
                            <th>Tarea</th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.NombreDocumento)</td>
                                <td>@Html.DisplayFor(modelItem => item.Extension)</td>
                                <td>@Html.DisplayFor(modelItem => item.FechaCreacion)</td>
                                <td>@Html.DisplayFor(modelItem => item.Estudiantes.Matricula)</td>
                                <td>@Html.DisplayFor(modelItem => item.Tareas.Nombre)</td>
                                <td class="text-end">
                                    <div class="btn-group btn-group-sm">
                                        @Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { @class = "btn btn-outline-primary" })
                                        @Html.ActionLink("Ver", "Details", new { id = item.Id }, new { @class = "btn btn-outline-info" })
                                        @Html.ActionLink("Eliminar", "Delete", new { id = item.Id }, new { @class = "btn btn-outline-danger" })
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card-footer bg-light">
            <div class="text-muted small">
                <i class="fas fa-file-alt me-1"></i> Total entregables: @Model.Count()
            </div>
        </div>
    </div>
</div>
